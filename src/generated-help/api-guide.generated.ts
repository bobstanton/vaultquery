// Generated by md-to-obsidian-dom
// Source: api-guide.md

import { MarkdownRenderer } from 'obsidian';
import './styles.generated.css';

import type { RenderContext } from './types.generated';

export const pageId = "api-guide";
export const pageTitle = "VaultQuery API Guide";

export const meta = {
  id: "api-guide",
  title: "VaultQuery API Guide",
};

export const dynamicKeys: string[] = [];
export const linkedPages: string[] = [];

/**
 * Render the "VaultQuery API Guide" help page
 * @param container - Container element to render into
 * @param ctx - Render context with code highlighting and dynamic content handlers
 */
export function render(container: HTMLElement, ctx: RenderContext): void {
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "VaultQuery API Guide" });
  const para = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para.appendText("This guide explains how to use VaultQuery from a third-party Obsidian plugin.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Getting the API" });
  const para1 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para1.appendText("VaultQuery exposes its API through the global ");
  para1.createEl('code', { cls: 'vaultquery-help-code', text: "app" });
  para1.appendText(" object. First, check if VaultQuery is installed and enabled:");
  const codeBlock = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Get the VaultQuery plugin instance\nconst vaultQuery = this.app.plugins.getPlugin('vaultquery');\n\nif (!vaultQuery) {\n  console.log('VaultQuery plugin is not installed or enabled');\n  return;\n}\n\n// Access the API\nconst api = vaultQuery.api;\n```", codeBlock, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "TypeScript Types" });
  const para2 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para2.appendText("Add VaultQuery as a dev dependency to import types:");
  const codeBlock1 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```bash\nnpm install --save-dev github:bobstanton/vaultquery\n```", codeBlock1, '', ctx.component);
  const para3 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para3.appendText("Then import the required types:");
  const codeBlock2 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nimport type {\n  IVaultQueryAPI,\n  VaultQueryPlugin,\n  QueryResult,\n  PreviewResult,\n  VaultQueryTableProvider\n} from 'vaultquery/api';\n\nconst vaultQuery = this.app.plugins.getPlugin('vaultquery') as VaultQueryPlugin | undefined;\nif (!vaultQuery?.api) return;\n\nconst results: QueryResult[] = await vaultQuery.api.query('SELECT * FROM notes');\n```", codeBlock2, '', ctx.component);
  const para4 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para4.appendText("Available types:");
  const tableWrapper = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table = tableWrapper.createEl('table', { cls: 'vaultquery-help-table' });
  const thead = table.createEl('thead');
  const row = thead.createEl('tr');
  row.createEl('th', { text: "Type" });
  row.createEl('th', { text: "Description" });
  const tbody = table.createEl('tbody');
  const row1 = tbody.createEl('tr');
  const td = row1.createEl('td');
  td.createEl('code', { cls: 'vaultquery-help-code', text: "VaultQueryPlugin" });
  const td1 = row1.createEl('td');
  td1.appendText("Plugin instance (use with ");
  td1.createEl('code', { cls: 'vaultquery-help-code', text: "getPlugin" });
  td1.appendText(")");
  const row2 = tbody.createEl('tr');
  const td2 = row2.createEl('td');
  td2.createEl('code', { cls: 'vaultquery-help-code', text: "IVaultQueryAPI" });
  row2.createEl('td', { text: "Full API interface" });
  const row3 = tbody.createEl('tr');
  const td3 = row3.createEl('td');
  td3.createEl('code', { cls: 'vaultquery-help-code', text: "QueryResult" });
  row3.createEl('td', { text: "Query result row" });
  const row4 = tbody.createEl('tr');
  const td4 = row4.createEl('td');
  td4.createEl('code', { cls: 'vaultquery-help-code', text: "PreviewResult" });
  row4.createEl('td', { text: "DML preview result" });
  const row5 = tbody.createEl('tr');
  const td5 = row5.createEl('td');
  td5.createEl('code', { cls: 'vaultquery-help-code', text: "NoteSource" });
  const td6 = row5.createEl('td');
  td6.appendText("`");
  td6.appendText("string \\");
  const td7 = row5.createEl('td');
  td7.appendText("TFile");
  td7.appendText("`");
  td7.appendText(" for template variables");
  const row6 = tbody.createEl('tr');
  const td8 = row6.createEl('td');
  td8.createEl('code', { cls: 'vaultquery-help-code', text: "FileIndexedEvent" });
  const td9 = row6.createEl('td');
  td9.appendText("Event data for ");
  td9.createEl('code', { cls: 'vaultquery-help-code', text: "file-indexed" });
  const row7 = tbody.createEl('tr');
  const td10 = row7.createEl('td');
  td10.createEl('code', { cls: 'vaultquery-help-code', text: "FileRemovedEvent" });
  const td11 = row7.createEl('td');
  td11.appendText("Event data for ");
  td11.createEl('code', { cls: 'vaultquery-help-code', text: "file-removed" });
  const row8 = tbody.createEl('tr');
  const td12 = row8.createEl('td');
  td12.createEl('code', { cls: 'vaultquery-help-code', text: "VaultIndexedEvent" });
  const td13 = row8.createEl('td');
  td13.appendText("Event data for ");
  td13.createEl('code', { cls: 'vaultquery-help-code', text: "vault-indexed" });
  const row9 = tbody.createEl('tr');
  const td14 = row9.createEl('td');
  td14.createEl('code', { cls: 'vaultquery-help-code', text: "EventRef" });
  row9.createEl('td', { text: "Event subscription reference" });
  const row10 = tbody.createEl('tr');
  const td15 = row10.createEl('td');
  td15.createEl('code', { cls: 'vaultquery-help-code', text: "IndexingStats" });
  row10.createEl('td', { text: "Performance statistics" });
  const row11 = tbody.createEl('tr');
  const td16 = row11.createEl('td');
  td16.createEl('code', { cls: 'vaultquery-help-code', text: "VaultQueryTableProvider" });
  row11.createEl('td', { text: "Third-party provider tables API contract" });
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Core API Methods" });
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Executing Queries" });
  const codeBlock3 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst results = await api.query('SELECT * FROM notes LIMIT 10');\n\nconst file = this.app.workspace.getActiveFile();\nconst results = await api.query(\n  'SELECT * FROM links WHERE path = {this.path}',\n  file\n);\n\nfor (const row of results) {\n  console.log(row.path, row.title);\n}\n```", codeBlock3, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Waiting for Indexing" });
  const para5 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para5.appendText("At plugin load time, VaultQuery may still be indexing the vault. Use ");
  para5.createEl('code', { cls: 'vaultquery-help-code', text: "waitForIndexing()" });
  para5.appendText(" to ensure complete data:");
  const codeBlock4 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nasync onload() {\n  const vaultQuery = this.app.plugins.getPlugin('vaultquery');\n  if (!vaultQuery) return;\n\n  await vaultQuery.api.waitForIndexing();\n\n  const notes = await vaultQuery.api.query('SELECT COUNT(*) as count FROM notes');\n  console.log(`Indexed ${notes[0].count} notes`);\n}\n```", codeBlock4, '', ctx.component);
  const para6 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para6.createEl('strong', { cls: 'vaultquery-help-bold', text: "Note" });
  para6.appendText(": ");
  para6.createEl('code', { cls: 'vaultquery-help-code', text: "query()" });
  para6.appendText(" does not wait call ");
  para6.createEl('code', { cls: 'vaultquery-help-code', text: "waitForIndexing()" });
  para6.appendText(" internally.");
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Checking Indexing Status" });
  const codeBlock5 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst status = api.getIndexingStatus();\n\nif (status.isIndexing) {\n  console.log(`Indexing: ${status.progress.current}/${status.progress.total}`);\n  console.log(`Current file: ${status.progress.currentFile}`);\n} else {\n  console.log('Indexing complete');\n}\n```", codeBlock5, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Triggering Reindexing" });
  const codeBlock6 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Incremental reindex (only modified files)\nawait api.reindexVault();\n\n// Full reindex (clears database and reindexes everything)\nawait api.forceReindexVault();\n\n// Reindex a specific note\nawait api.reindexNote('folder/my-note.md');\n```", codeBlock6, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Write Operations" });
  const para7 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para7.appendText("VaultQuery supports INSERT, UPDATE, and DELETE operations that sync changes back to markdown files. Write operations require \"Allow write operations\" enabled in VaultQuery settings.");
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Preview and Apply Pattern" });
  const codeBlock7 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Step 1: Preview the changes\nconst preview = await api.previewQuery(`\n  UPDATE tasks\n  SET status = 'DONE', done_date = date('now')\n  WHERE path = 'projects/todo.md' AND task_text LIKE '%review%'\n`);\n\n// Step 2: Inspect the preview\nconsole.log(`Operation: ${preview.operationType}`);\nconsole.log(`Affected rows: ${preview.affectedRowCount}`);\nconsole.log('Before:', preview.beforeRows);\nconsole.log('After:', preview.afterRows);\n\n// Step 3: Apply if satisfied\nif (preview.affectedRowCount > 0) {\n  const affectedPaths = await api.applyPreview(preview);\n  console.log('Modified files:', affectedPaths);\n}\n```", codeBlock7, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Insert Example" });
  const codeBlock8 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Create a new note\nconst preview = await api.previewQuery(`\n  INSERT INTO notes (path, title, content)\n  VALUES ('new-note.md', 'New Note', '# New Note\\n\\nContent here')\n`);\n\nawait api.applyPreview(preview);\n```", codeBlock8, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Update Example" });
  const codeBlock9 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Mark tasks as complete\nconst preview = await api.previewQuery(`\n  UPDATE tasks\n  SET status = 'DONE', done_date = date('now')\n  WHERE status = 'TODO' AND due_date < date('now')\n`);\n\nif (preview.affectedRowCount > 0) {\n  await api.applyPreview(preview);\n}\n```", codeBlock9, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Delete Example" });
  const codeBlock10 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Delete old notes (requires \"Allow file deletion\" setting)\nconst preview = await api.previewQuery(`\n  DELETE FROM notes\n  WHERE path LIKE 'archive/%'\n    AND modified < strftime('%s', 'now', '-1 year') * 1000\n`);\n\nawait api.applyPreview(preview);\n```", codeBlock10, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Custom SQL Functions" });
  const para8 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para8.appendText("Register custom JavaScript functions for use in SQL queries:");
  const codeBlock11 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Register a simple function\napi.registerCustomFunction('reverse', `\n  function(str) {\n    if (str === null) return null;\n    return String(str).split('').reverse().join('');\n  }\n`);\n\n// Use in queries\nconst results = await api.query(`\n  SELECT title, reverse(title) as reversed\n  FROM notes\n  LIMIT 5\n`);\n\n// Register a function with multiple parameters\napi.registerCustomFunction('concat_with', `\n  function(separator, ...args) {\n    return args.filter(a => a !== null).join(separator || '');\n  }\n`);\n```", codeBlock11, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Executing DDL Statements" });
  const para9 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para9.appendText("Use ");
  para9.createEl('code', { cls: 'vaultquery-help-code', text: "execute()" });
  para9.appendText(" for statements that don't return query results. It returns the number of rows affected:");
  const codeBlock12 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Create a custom view (returns 0 for DDL)\nconst affected = await api.execute(`\n  CREATE VIEW IF NOT EXISTS recent_notes AS\n  SELECT path, title, modified\n  FROM notes\n  WHERE modified > strftime('%s', 'now', '-7 days') * 1000\n  ORDER BY modified DESC\n`);\n\n// Query the view\nconst recent = await api.query('SELECT * FROM recent_notes');\n```", codeBlock12, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Checking Capabilities" });
  const para10 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para10.appendText("Before using certain features, check what's available based on user settings:");
  const codeBlock13 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst caps = api.getCapabilities();\n\n// Check if write operations are enabled\nif (caps.writeEnabled) {\n  const preview = await api.previewQuery('UPDATE tasks SET status = \"DONE\"...');\n  await api.applyPreview(preview);\n} else {\n  new Notice('Write operations are disabled in VaultQuery settings');\n}\n\n// Check if specific data is indexed\nif (caps.indexing.tasks) {\n  const tasks = await api.query('SELECT * FROM tasks WHERE status = \"TODO\"');\n}\n\nif (caps.indexing.headings) {\n  const headings = await api.query('SELECT * FROM headings WHERE level = 1');\n}\n\n// Check if file deletion is allowed\nif (caps.fileDeleteEnabled) {\n  // Can use DELETE FROM notes\n}\n```", codeBlock13, '', ctx.component);
  const para11 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para11.appendText("The capabilities object includes:");
  const tableWrapper1 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table1 = tableWrapper1.createEl('table', { cls: 'vaultquery-help-table' });
  const thead1 = table1.createEl('thead');
  const row12 = thead1.createEl('tr');
  row12.createEl('th', { text: "Property" });
  row12.createEl('th', { text: "Description" });
  const tbody1 = table1.createEl('tbody');
  const row13 = tbody1.createEl('tr');
  const td17 = row13.createEl('td');
  td17.createEl('code', { cls: 'vaultquery-help-code', text: "writeEnabled" });
  row13.createEl('td', { text: "INSERT/UPDATE/DELETE operations are allowed" });
  const row14 = tbody1.createEl('tr');
  const td18 = row14.createEl('td');
  td18.createEl('code', { cls: 'vaultquery-help-code', text: "fileDeleteEnabled" });
  row14.createEl('td', { text: "DELETE FROM notes (file deletion) is allowed" });
  const row15 = tbody1.createEl('tr');
  const td19 = row15.createEl('td');
  td19.createEl('code', { cls: 'vaultquery-help-code', text: "thirdPartyProviderTablesEnabled" });
  row15.createEl('td', { text: "Third-party provider tables are enabled" });
  const row16 = tbody1.createEl('tr');
  const td20 = row16.createEl('td');
  td20.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.content" });
  row16.createEl('td', { text: "Note content is indexed" });
  const row17 = tbody1.createEl('tr');
  const td21 = row17.createEl('td');
  td21.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.frontmatter" });
  const td22 = row17.createEl('td');
  td22.createEl('code', { cls: 'vaultquery-help-code', text: "properties" });
  td22.appendText(" table is available");
  const row18 = tbody1.createEl('tr');
  const td23 = row18.createEl('td');
  td23.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.tables" });
  const td24 = row18.createEl('td');
  td24.createEl('code', { cls: 'vaultquery-help-code', text: "table_cells" });
  td24.appendText(" table is available");
  const row19 = tbody1.createEl('tr');
  const td25 = row19.createEl('td');
  td25.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.tasks" });
  const td26 = row19.createEl('td');
  td26.createEl('code', { cls: 'vaultquery-help-code', text: "tasks" });
  td26.appendText(" table is available");
  const row20 = tbody1.createEl('tr');
  const td27 = row20.createEl('td');
  td27.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.headings" });
  const td28 = row20.createEl('td');
  td28.createEl('code', { cls: 'vaultquery-help-code', text: "headings" });
  td28.appendText(" table is available");
  const row21 = tbody1.createEl('tr');
  const td29 = row21.createEl('td');
  td29.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.links" });
  const td30 = row21.createEl('td');
  td30.createEl('code', { cls: 'vaultquery-help-code', text: "links" });
  td30.appendText(" table is available");
  const row22 = tbody1.createEl('tr');
  const td31 = row22.createEl('td');
  td31.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.tags" });
  const td32 = row22.createEl('td');
  td32.createEl('code', { cls: 'vaultquery-help-code', text: "tags" });
  td32.appendText(" table is available");
  const row23 = tbody1.createEl('tr');
  const td33 = row23.createEl('td');
  td33.createEl('code', { cls: 'vaultquery-help-code', text: "indexing.listItems" });
  const td34 = row23.createEl('td');
  td34.createEl('code', { cls: 'vaultquery-help-code', text: "list_items" });
  td34.appendText(" table is available");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Schema Information" });
  const para12 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para12.appendText("Get the current database schema as markdown:");
  const codeBlock14 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst schemaMarkdown = await api.getSchemaInfo();\nconsole.log(schemaMarkdown);\n// Returns formatted markdown with all tables, columns, and types\n```", codeBlock14, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Third-party provider tables" });
  const para13 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para13.appendText("Third-party plugins can register third-party provider tables when ");
  para13.createEl('strong', { cls: 'vaultquery-help-bold', text: "Third-party provider tables" });
  para13.appendText(" is enabled in VaultQuery settings.");
  const para14 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para14.appendText("Third-party provider tables are useful for external or plugin-owned datasets such as weather, tides, calendars, RSS feeds, bookmarks, or API-backed records. VaultQuery owns SQLite table creation, status tracking, generic rendered definition blocks, and query access. The provider plugin owns parsing, fetching, caching, normalization, and returned rows.");
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Registering Providers" });
  const para15 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para15.appendText("Use ");
  para15.createEl('code', { cls: 'vaultquery-help-code', text: "registerVaultQueryTableProviders()" });
  para15.appendText(" from plugin startup code. It waits for VaultQuery with mobile-safe retries, checks whether the feature is enabled, registers every provider, re-registers after mobile database recovery, and returns a handle you can dispose during plugin unload.");
  const codeBlock15 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nimport {\n  registerVaultQueryTableProviders,\n  type ManagedVaultQueryTableProviderRegistration\n} from 'vaultquery/api';\n\nprivate vaultQueryTables: ManagedVaultQueryTableProviderRegistration | null = null;\n\nasync onload() {\n  this.vaultQueryTables = await registerVaultQueryTableProviders(this.app, {\n    pluginId: 'places-environment',\n    providers: [\n      createPlacesWeatherProvider(this.weatherDataService),\n      createPlacesTideProvider(this.tideDataService),\n      createPlacesSolarProvider(this)\n    ],\n    logger: {\n      info: message => Logger.info('Places', message),\n      warn: message => Logger.warn('Places', message),\n      error: (message, error) => Logger.error('Places', message, error)\n    }\n  });\n}\n\nasync onunload() {\n  await this.vaultQueryTables?.dispose();\n}\n```", codeBlock15, '', ctx.component);
  const para16 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para16.createEl('code', { cls: 'vaultquery-help-code', text: "registerTableProvider()" });
  para16.appendText(" remains available for advanced integrations that need to own their own lifecycle.");
  const codeBlock16 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nimport type { VaultQueryTableProvider } from 'vaultquery/api';\n\nconst provider: VaultQueryTableProvider = {\n  id: 'places-environment.weather',\n  displayName: 'Places Environment Weather',\n  tables: [\n    {\n      name: 'weather_daily',\n      description: 'Daily weather rows from Places Environment.',\n      primaryKey: ['id', 'date', 'units'],\n      defaultStaleAfterMs: 2 * 60 * 60 * 1000,\n      columns: [\n        { name: 'id', type: 'TEXT', description: 'Stable location identifier.' },\n        { name: 'name', type: 'TEXT', nullable: true },\n        { name: 'date', type: 'TEXT' },\n        { name: 'units', type: 'TEXT' },\n        { name: 'temperature_2m_max', type: 'REAL', nullable: true },\n        { name: 'temperature_2m_min', type: 'REAL', nullable: true }\n      ],\n      indexes: [\n        { name: 'idx_weather_daily_id_date', columns: ['id', 'date'] }\n      ]\n    }\n  ],\n  definitionBlock: {\n    language: 'places-weather-vaultquery',\n    parse: (source, context) => parseWeatherDefinition(source, context)\n  },\n  refresh: async (context) => refreshWeatherRows(context)\n};\n\nawait api.registerTableProvider(provider);\n```", codeBlock16, '', ctx.component);
  const para17 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para17.appendText("VaultQuery discovers matching fenced code blocks during indexing, calls the provider parser, stores refresh definitions in memory, and renders a generic status block with the standard floating refresh button.");
  const para18 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para18.appendText("Registered provider definition languages also participate in VaultQuery editor behavior:");
  const list = container.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item.appendText("Provider blocks use generic config-style highlighting rather than VaultQuery-owned config semantics");
  const item1 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item1.appendText("VaultQuery currently ships provider-definition key/value autocomplete for the built-in Places provider block languages (");
  item1.createEl('code', { cls: 'vaultquery-help-code', text: "places-weather-vaultquery" });
  item1.appendText(", ");
  item1.createEl('code', { cls: 'vaultquery-help-code', text: "places-tides-vaultquery" });
  item1.appendText(", and ");
  item1.createEl('code', { cls: 'vaultquery-help-code', text: "places-solar-vaultquery" });
  item1.appendText(")");
  const item2 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item2.appendText("Other registered provider block languages get generic highlighting and rendered status controls, but there is not yet a public provider-specific autocomplete extension point");
  const item3 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item3.appendText("Normal ");
  item3.createEl('code', { cls: 'vaultquery-help-code', text: "vaultquery" });
  item3.appendText(" SQL blocks can autocomplete third-party provider tables and columns once those tables exist in the schema");
  const item4 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item4.appendText("SQL autocomplete is clause-aware and alias-aware, so third-party provider tables participate in relation and column suggestions the same way built-in tables do");
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Refresh Results" });
  const para19 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para19.appendText("Provider refresh callbacks return rows grouped by table name:");
  const codeBlock17 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nreturn {\n  tables: {\n    weather_daily: {\n      replaceWhere: { id: 'scranton-pa', units: 'imperial' },\n      rows: [\n        {\n          id: 'scranton-pa',\n          name: 'Scranton, PA',\n          date: '2026-04-21',\n          units: 'imperial',\n          temperature_2m_max: 68,\n          temperature_2m_min: 42\n        }\n      ]\n    }\n  }\n};\n```", codeBlock17, '', ctx.component);
  const para20 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "replaceWhere" });
  para20.appendText(" is optional. When present, VaultQuery deletes rows matching those declared column values inside the same transaction before upserting the returned rows.");
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Status and Cleanup" });
  const codeBlock18 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst status = await api.getTableProviderStatus('places-environment.weather');\nconsole.table(status);\n\nawait api.unregisterTableProvider('places-environment.weather');\n```", codeBlock18, '', ctx.component);
  const para21 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para21.appendText("Provider rows remain queryable after unregistering. Refresh attempts are blocked until the provider registers again.");
  const para22 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para22.appendText("Schema output reports third-party provider table rows from VaultQuery's provider refresh status. For providers with multiple refresh definitions or upserted partitions, the displayed row count represents the largest recorded refresh result for that table, not necessarily ");
  para22.createEl('code', { cls: 'vaultquery-help-code', text: "SELECT COUNT(*)" });
  para22.appendText(" over the physical table.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Utility Methods" });
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Check if a File Should Be Indexed" });
  const codeBlock19 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst file = this.app.vault.getAbstractFileByPath('notes/example.md');\nif (file instanceof TFile && api.shouldIndexFile(file)) {\n  console.log('This file will be indexed');\n}\n```", codeBlock19, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Check if a File Needs Reindexing" });
  const codeBlock20 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst file = this.app.vault.getAbstractFileByPath('notes/example.md');\nif (file instanceof TFile) {\n  const needsUpdate = await api.needsIndexing(file);\n  if (needsUpdate) {\n    await api.indexNote(file);\n  }\n}\n```", codeBlock20, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Get All Indexed Files" });
  const codeBlock21 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst indexedFiles = await api.getIndexedFiles();\nfor (const file of indexedFiles) {\n  console.log(`${file.path} - modified: ${new Date(file.modified)}`);\n}\n```", codeBlock21, '', ctx.component);
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "Remove a Note from Index" });
  const codeBlock22 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\n// Remove without deleting the file\napi.removeNote('notes/removed.md');\n```", codeBlock22, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Template Variables" });
  const para23 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para23.appendText("When passing a ");
  para23.createEl('code', { cls: 'vaultquery-help-code', text: "noteSource" });
  para23.appendText(" (TFile or path string), these template variables are available:");
  const tableWrapper2 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table2 = tableWrapper2.createEl('table', { cls: 'vaultquery-help-table' });
  const thead2 = table2.createEl('thead');
  const row24 = thead2.createEl('tr');
  row24.createEl('th', { text: "Variable" });
  row24.createEl('th', { text: "Description" });
  const tbody2 = table2.createEl('tbody');
  const row25 = tbody2.createEl('tr');
  const td35 = row25.createEl('td');
  td35.createEl('code', { cls: 'vaultquery-help-code', text: "{this.path}" });
  row25.createEl('td', { text: "Full file path" });
  const row26 = tbody2.createEl('tr');
  const td36 = row26.createEl('td');
  td36.createEl('code', { cls: 'vaultquery-help-code', text: "{this.name}" });
  row26.createEl('td', { text: "Filename with extension" });
  const row27 = tbody2.createEl('tr');
  const td37 = row27.createEl('td');
  td37.createEl('code', { cls: 'vaultquery-help-code', text: "{this.basename}" });
  row27.createEl('td', { text: "Filename without extension" });
  const row28 = tbody2.createEl('tr');
  const td38 = row28.createEl('td');
  td38.createEl('code', { cls: 'vaultquery-help-code', text: "{this.extension}" });
  row28.createEl('td', { text: "File extension" });
  const row29 = tbody2.createEl('tr');
  const td39 = row29.createEl('td');
  td39.createEl('code', { cls: 'vaultquery-help-code', text: "{this.folder}" });
  row29.createEl('td', { text: "Parent folder path" });
  const row30 = tbody2.createEl('tr');
  const td40 = row30.createEl('td');
  td40.createEl('code', { cls: 'vaultquery-help-code', text: "{this.created}" });
  row30.createEl('td', { text: "Creation timestamp (ms)" });
  const row31 = tbody2.createEl('tr');
  const td41 = row31.createEl('td');
  td41.createEl('code', { cls: 'vaultquery-help-code', text: "{this.modified}" });
  row31.createEl('td', { text: "Modified timestamp (ms)" });
  const row32 = tbody2.createEl('tr');
  const td42 = row32.createEl('td');
  td42.createEl('code', { cls: 'vaultquery-help-code', text: "{this.size}" });
  row32.createEl('td', { text: "File size in bytes" });
  const row33 = tbody2.createEl('tr');
  const td43 = row33.createEl('td');
  td43.createEl('code', { cls: 'vaultquery-help-code', text: "{this.frontmatter.key}" });
  row33.createEl('td', { text: "Frontmatter property value" });
  const para24 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para24.appendText("Example:");
  const codeBlock23 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\nconst file = this.app.workspace.getActiveFile();\n\n// Find notes in the same folder\nconst siblings = await api.query(`\n  SELECT path, title FROM notes\n  WHERE path LIKE '{this.folder}/%'\n    AND path != '{this.path}'\n`, file);\n\n// Find notes linking to current note\nconst backlinks = await api.query(`\n  SELECT path, link_text FROM links\n  WHERE link_target_path = '{this.path}'\n`, file);\n```", codeBlock23, '', ctx.component);
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Error Handling" });
  const codeBlock24 = container.createDiv();
  MarkdownRenderer.render(ctx.app, "```typescript\ntry {\n  const results = await api.query('SELECT * FROM invalid_table');\n} catch (error) {\n  // Error messages include helpful hints\n  // e.g., \"no such table: properties\n  //        Note: Property indexing is disabled. Enable it in Settings...\"\n  console.error(error.message);\n}\n\n// Check if write operations are allowed before previewing\ntry {\n  const preview = await api.previewQuery('UPDATE notes SET title = \"test\"');\n} catch (error) {\n  if (error.message.includes('Write operations are disabled')) {\n    // Prompt user to enable in settings\n  }\n}\n```", codeBlock24, '', ctx.component);
}