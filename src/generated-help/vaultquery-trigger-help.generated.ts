// Generated by md-to-obsidian-dom
// Source: vaultquery-trigger-help.md

import './styles.generated.css';

import type { RenderContext } from './types.generated';

export const pageId = "vaultquery-trigger-help";
export const pageTitle = "VaultQuery Trigger Reference";

export const meta = {
  id: "vaultquery-trigger-help",
  title: "VaultQuery Trigger Reference",
};

export const dynamicKeys: string[] = ["triggers"];
export const linkedPages: string[] = [];

/**
 * Render the "VaultQuery Trigger Reference" help page
 * @param container - Container element to render into
 * @param ctx - Render context with code highlighting and dynamic content handlers
 */
export function render(container: HTMLElement, ctx: RenderContext): void {
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "VaultQuery Triggers" });
  const para = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para.appendText("Triggers automate actions when notes are created, updated, or deleted. They use SQLite trigger syntax and can either queue file modifications via ");
  para.createEl('code', { cls: 'vaultquery-help-code', text: "vq_*" });
  para.appendText(" functions or make database-only changes with direct SQL.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "How Triggers Work" });
  const list = container.createEl('ol', { cls: 'vaultquery-help-list vaultquery-help-ordered-list' });
  const item = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item.appendText("Define a trigger in a ");
  item.createEl('code', { cls: 'vaultquery-help-code', text: "vaultquery-trigger" });
  item.appendText(" code block");
  const item1 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item1.appendText("When a note is indexed, the trigger SQL is registered with the database");
  const item2 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item2.appendText("When data changes match the trigger condition, the trigger body executes");
  const item3 = list.createEl('li', { cls: 'vaultquery-help-list-item' });
  item3.appendText("If using ");
  item3.createEl('code', { cls: 'vaultquery-help-code', text: "vq_*" });
  item3.appendText(" functions, file modifications are applied after indexing completes");
  const callout = container.createDiv({ cls: 'callout', attr: { 'data-callout': "important", 'data-callout-metadata': '', 'data-callout-fold': '' } });
  const header = callout.createDiv({ cls: 'callout-title', attr: { dir: 'auto' } });
  const icon = header.createDiv({ cls: 'callout-icon' });
  ctx.setIcon(icon, "alert-circle");
  header.createDiv({ cls: 'callout-title-inner', text: "Timing" });
  const content = callout.createDiv({ cls: 'callout-content' });
  const para1 = content.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para1.appendText("Changes made by triggers do not trigger other triggers.");
  const callout1 = container.createDiv({ cls: 'callout', attr: { 'data-callout': "note", 'data-callout-metadata': '', 'data-callout-fold': '' } });
  const header1 = callout1.createDiv({ cls: 'callout-title', attr: { dir: 'auto' } });
  const icon1 = header1.createDiv({ cls: 'callout-icon' });
  ctx.setIcon(icon1, "pencil");
  header1.createDiv({ cls: 'callout-title-inner', text: "INSERT vs UPDATE Semantics" });
  const content1 = callout1.createDiv({ cls: 'callout-content' });
  const para2 = content1.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para2.appendText("During indexing, VaultQuery properly distinguishes between new and modified records:");
  const list1 = content1.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item4 = list1.createEl('li', { cls: 'vaultquery-help-list-item' });
  item4.createEl('strong', { cls: 'vaultquery-help-bold', text: "notes" });
  item4.appendText(": AFTER INSERT fires for new files, AFTER UPDATE fires when file content changes");
  const item5 = list1.createEl('li', { cls: 'vaultquery-help-list-item' });
  item5.createEl('strong', { cls: 'vaultquery-help-bold', text: "properties" });
  item5.appendText(": AFTER INSERT fires for new properties, AFTER UPDATE fires when values change");
  const item6 = list1.createEl('li', { cls: 'vaultquery-help-list-item' });
  item6.createEl('strong', { cls: 'vaultquery-help-bold', text: "tags" });
  item6.appendText(": AFTER INSERT fires only for genuinely new tag occurrences. Line number shifts (e.g., from editing above a tag) are handled as updates without firing INSERT triggers.");
  const item7 = list1.createEl('li', { cls: 'vaultquery-help-list-item' });
  item7.createEl('strong', { cls: 'vaultquery-help-bold', text: "headings" });
  item7.appendText(": AFTER INSERT fires only for new heading text. Existing headings that move (line number changes) are updated without firing INSERT triggers.");
  const item8 = list1.createEl('li', { cls: 'vaultquery-help-list-item' });
  item8.createEl('strong', { cls: 'vaultquery-help-bold', text: "tasks, list_items" });
  item8.appendText(": AFTER INSERT/UPDATE fires correctly when items have a ");
  item8.createEl('code', { cls: 'vaultquery-help-code', text: "block_id" });
  item8.appendText(" or ");
  item8.createEl('code', { cls: 'vaultquery-help-code', text: "anchor_hash" });
  item8.appendText(". Items without these identifiers use line-based fallback matching.");
  const para3 = content1.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para3.appendText("This allows triggers to respond differently to new vs modified data.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Two Approaches" });
  const heading = container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3' });
  heading.appendText("1. File-Modifying Functions (");
  heading.createEl('code', { cls: 'vaultquery-help-code', text: "vq_*" });
  heading.appendText(")");
  const para4 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para4.appendText("These functions queue actions that modify actual markdown files:");
  container.createEl('h4', { cls: 'vaultquery-help-heading vaultquery-help-h4', text: "General Functions" });
  const tableWrapper = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table = tableWrapper.createEl('table', { cls: 'vaultquery-help-table' });
  const thead = table.createEl('thead');
  const row = thead.createEl('tr');
  row.createEl('th', { text: "Function" });
  row.createEl('th', { text: "Description" });
  const tbody = table.createEl('tbody');
  const row1 = tbody.createEl('tr');
  const td = row1.createEl('td');
  td.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_property(path, key, value)" });
  row1.createEl('td', { text: "Set a frontmatter property" });
  const row2 = tbody.createEl('tr');
  const td1 = row2.createEl('td');
  td1.createEl('code', { cls: 'vaultquery-help-code', text: "vq_remove_property(path, key)" });
  row2.createEl('td', { text: "Remove a frontmatter property" });
  const row3 = tbody.createEl('tr');
  const td2 = row3.createEl('td');
  td2.createEl('code', { cls: 'vaultquery-help-code', text: "vq_rename_note(path, new_name)" });
  row3.createEl('td', { text: "Rename a note (keeps same folder)" });
  const row4 = tbody.createEl('tr');
  const td3 = row4.createEl('td');
  td3.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_content(path, content)" });
  row4.createEl('td', { text: "Replace entire file content (preserves frontmatter)" });
  const row5 = tbody.createEl('tr');
  const td4 = row5.createEl('td');
  td4.createEl('code', { cls: 'vaultquery-help-code', text: "vq_replace_content(path, search, replacement)" });
  row5.createEl('td', { text: "Replace text in file content" });
  const row6 = tbody.createEl('tr');
  const td5 = row6.createEl('td');
  td5.createEl('code', { cls: 'vaultquery-help-code', text: "vq_notify(message)" });
  row6.createEl('td', { text: "Show an Obsidian notification" });
  const row7 = tbody.createEl('tr');
  const td6 = row7.createEl('td');
  td6.createEl('code', { cls: 'vaultquery-help-code', text: "vq_log(message)" });
  row7.createEl('td', { text: "Log to developer console" });
  const row8 = tbody.createEl('tr');
  const td7 = row8.createEl('td');
  td7.createEl('code', { cls: 'vaultquery-help-code', text: "vq_debounce(key, ms)" });
  const td8 = row8.createEl('td');
  td8.appendText("Returns 1 if ");
  td8.createEl('code', { cls: 'vaultquery-help-code', text: "ms" });
  td8.appendText(" milliseconds have passed since last call with this ");
  td8.createEl('code', { cls: 'vaultquery-help-code', text: "key" });
  td8.appendText(", 0 otherwise. Use in WHEN clause to debounce triggers (leading edge).");
  const row9 = tbody.createEl('tr');
  const td9 = row9.createEl('td');
  td9.createEl('code', { cls: 'vaultquery-help-code', text: "vq_defer(key, ms)" });
  const td10 = row9.createEl('td');
  td10.appendText("Trailing-edge debounce: defers all subsequent ");
  td10.createEl('code', { cls: 'vaultquery-help-code', text: "vq_*" });
  td10.appendText(" calls in the trigger body until ");
  td10.createEl('code', { cls: 'vaultquery-help-code', text: "ms" });
  td10.appendText(" milliseconds of idle time passes. Call at START of trigger body.");
  container.createEl('h4', { cls: 'vaultquery-help-heading vaultquery-help-h4', text: "Task Functions" });
  const tableWrapper1 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table1 = tableWrapper1.createEl('table', { cls: 'vaultquery-help-table' });
  const thead1 = table1.createEl('thead');
  const row10 = thead1.createEl('tr');
  row10.createEl('th', { text: "Function" });
  row10.createEl('th', { text: "Description" });
  const tbody1 = table1.createEl('tbody');
  const row11 = tbody1.createEl('tr');
  const td11 = row11.createEl('td');
  td11.createEl('code', { cls: 'vaultquery-help-code', text: "vq_complete_task(path, line_number)" });
  const td12 = row11.createEl('td');
  td12.appendText("Mark a task as done (");
  td12.createEl('code', { cls: 'vaultquery-help-code', text: "[x]" });
  td12.appendText(")");
  const row12 = tbody1.createEl('tr');
  const td13 = row12.createEl('td');
  td13.createEl('code', { cls: 'vaultquery-help-code', text: "vq_uncomplete_task(path, line_number)" });
  const td14 = row12.createEl('td');
  td14.appendText("Mark a task as todo (");
  td14.createEl('code', { cls: 'vaultquery-help-code', text: "[ ]" });
  td14.appendText(")");
  const row13 = tbody1.createEl('tr');
  const td15 = row13.createEl('td');
  td15.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_task_status(path, line_number, status)" });
  row13.createEl('td', { text: "Set task status (DONE, TODO, IN_PROGRESS, etc.)" });
  const row14 = tbody1.createEl('tr');
  const td16 = row14.createEl('td');
  td16.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_task_text(path, line_number, text)" });
  row14.createEl('td', { text: "Update task text (preserves checkbox status)" });
  const row15 = tbody1.createEl('tr');
  const td17 = row15.createEl('td');
  td17.createEl('code', { cls: 'vaultquery-help-code', text: "vq_add_task(path, text, after_line)" });
  row15.createEl('td', { text: "Add a new task after specified line (0 = start of file)" });
  const row16 = tbody1.createEl('tr');
  const td18 = row16.createEl('td');
  td18.createEl('code', { cls: 'vaultquery-help-code', text: "vq_delete_task(path, line_number)" });
  row16.createEl('td', { text: "Delete a task at the specified line" });
  container.createEl('h4', { cls: 'vaultquery-help-heading vaultquery-help-h4', text: "Heading Functions" });
  const tableWrapper2 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table2 = tableWrapper2.createEl('table', { cls: 'vaultquery-help-table' });
  const thead2 = table2.createEl('thead');
  const row17 = thead2.createEl('tr');
  row17.createEl('th', { text: "Function" });
  row17.createEl('th', { text: "Description" });
  const tbody2 = table2.createEl('tbody');
  const row18 = tbody2.createEl('tr');
  const td19 = row18.createEl('td');
  td19.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_heading_text(path, line_number, text)" });
  row18.createEl('td', { text: "Update heading text (preserves level)" });
  const row19 = tbody2.createEl('tr');
  const td20 = row19.createEl('td');
  td20.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_heading_level(path, line_number, level)" });
  row19.createEl('td', { text: "Change heading level (1-6)" });
  const row20 = tbody2.createEl('tr');
  const td21 = row20.createEl('td');
  td21.createEl('code', { cls: 'vaultquery-help-code', text: "vq_add_heading(path, level, text, after_line)" });
  row20.createEl('td', { text: "Add a new heading after specified line" });
  const row21 = tbody2.createEl('tr');
  const td22 = row21.createEl('td');
  td22.createEl('code', { cls: 'vaultquery-help-code', text: "vq_delete_heading(path, line_number)" });
  row21.createEl('td', { text: "Delete a heading at the specified line" });
  container.createEl('h4', { cls: 'vaultquery-help-heading vaultquery-help-h4', text: "List Item Functions" });
  const tableWrapper3 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table3 = tableWrapper3.createEl('table', { cls: 'vaultquery-help-table' });
  const thead3 = table3.createEl('thead');
  const row22 = thead3.createEl('tr');
  row22.createEl('th', { text: "Function" });
  row22.createEl('th', { text: "Description" });
  const tbody3 = table3.createEl('tbody');
  const row23 = tbody3.createEl('tr');
  const td23 = row23.createEl('td');
  td23.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_list_item_text(path, line_number, text)" });
  row23.createEl('td', { text: "Update list item text (preserves marker)" });
  const row24 = tbody3.createEl('tr');
  const td24 = row24.createEl('td');
  td24.createEl('code', { cls: 'vaultquery-help-code', text: "vq_add_list_item(path, text, after_line)" });
  row24.createEl('td', { text: "Add a new list item after specified line" });
  const row25 = tbody3.createEl('tr');
  const td25 = row25.createEl('td');
  td25.createEl('code', { cls: 'vaultquery-help-code', text: "vq_delete_list_item(path, line_number)" });
  row25.createEl('td', { text: "Delete a list item at the specified line" });
  container.createEl('h4', { cls: 'vaultquery-help-heading vaultquery-help-h4', text: "Table Functions" });
  const tableWrapper4 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table4 = tableWrapper4.createEl('table', { cls: 'vaultquery-help-table' });
  const thead4 = table4.createEl('thead');
  const row26 = thead4.createEl('tr');
  row26.createEl('th', { text: "Function" });
  row26.createEl('th', { text: "Description" });
  const tbody4 = table4.createEl('tbody');
  const row27 = tbody4.createEl('tr');
  const td26 = row27.createEl('td');
  td26.createEl('code', { cls: 'vaultquery-help-code', text: "vq_add_table_row(path, table_index, values_json)" });
  const td27 = row27.createEl('td');
  td27.appendText("Add a row to a markdown table. ");
  td27.createEl('code', { cls: 'vaultquery-help-code', text: "values_json" });
  td27.appendText(" is a JSON object mapping column names to values, e.g., ");
  td27.createEl('code', { cls: 'vaultquery-help-code', text: "'{\"Name\": \"John\", \"Age\": \"30\"}'" });
  const row28 = tbody4.createEl('tr');
  const td28 = row28.createEl('td');
  td28.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_table_cell(path, table_index, row_index, column_name, value)" });
  const td29 = row28.createEl('td');
  td29.appendText("Update a specific cell in a table. ");
  td29.createEl('code', { cls: 'vaultquery-help-code', text: "row_index" });
  td29.appendText(" is 0-based from data rows (excludes header/separator).");
  const row29 = tbody4.createEl('tr');
  const td30 = row29.createEl('td');
  td30.createEl('code', { cls: 'vaultquery-help-code', text: "vq_delete_table_row(path, table_index, row_index)" });
  const td31 = row29.createEl('td');
  td31.appendText("Delete a row from a table. ");
  td31.createEl('code', { cls: 'vaultquery-help-code', text: "row_index" });
  td31.appendText(" is 0-based from data rows.");
  container.createEl('h4', { cls: 'vaultquery-help-heading vaultquery-help-h4', text: "Note Functions" });
  const tableWrapper5 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table5 = tableWrapper5.createEl('table', { cls: 'vaultquery-help-table' });
  const thead5 = table5.createEl('thead');
  const row30 = thead5.createEl('tr');
  row30.createEl('th', { text: "Function" });
  row30.createEl('th', { text: "Description" });
  const tbody5 = table5.createEl('tbody');
  const row31 = tbody5.createEl('tr');
  const td32 = row31.createEl('td');
  td32.createEl('code', { cls: 'vaultquery-help-code', text: "vq_create_note(path, content)" });
  row31.createEl('td', { text: "Create a new note at the specified path. Does nothing if the file already exists. Creates parent folders if needed." });
  container.createEl('h3', { cls: 'vaultquery-help-heading vaultquery-help-h3', text: "2. Direct SQL (Auto-Synced)" });
  const para5 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para5.appendText("Regular SQL statements can also be used in triggers. Changes to the following are ");
  para5.createEl('strong', { cls: 'vaultquery-help-bold', text: "automatically synced back to files" });
  para5.appendText(":");
  const tableWrapper6 = container.createEl('div', { cls: 'vaultquery-help-table-wrapper' });
  const table6 = tableWrapper6.createEl('table', { cls: 'vaultquery-help-table' });
  const thead6 = table6.createEl('thead');
  const row32 = thead6.createEl('tr');
  row32.createEl('th', { text: "Table" });
  row32.createEl('th', { text: "Auto-Synced Columns" });
  const tbody6 = table6.createEl('tbody');
  const row33 = tbody6.createEl('tr');
  const td33 = row33.createEl('td');
  td33.createEl('code', { cls: 'vaultquery-help-code', text: "notes" });
  const td34 = row33.createEl('td');
  td34.createEl('code', { cls: 'vaultquery-help-code', text: "content" });
  const row34 = tbody6.createEl('tr');
  const td35 = row34.createEl('td');
  td35.createEl('code', { cls: 'vaultquery-help-code', text: "properties" });
  const td36 = row34.createEl('td');
  td36.createEl('code', { cls: 'vaultquery-help-code', text: "value" });
  const row35 = tbody6.createEl('tr');
  const td37 = row35.createEl('td');
  td37.createEl('code', { cls: 'vaultquery-help-code', text: "tasks" });
  const td38 = row35.createEl('td');
  td38.createEl('code', { cls: 'vaultquery-help-code', text: "status" });
  td38.appendText(", ");
  td38.createEl('code', { cls: 'vaultquery-help-code', text: "task_text" });
  const row36 = tbody6.createEl('tr');
  const td39 = row36.createEl('td');
  td39.createEl('code', { cls: 'vaultquery-help-code', text: "headings" });
  const td40 = row36.createEl('td');
  td40.createEl('code', { cls: 'vaultquery-help-code', text: "level" });
  td40.appendText(", ");
  td40.createEl('code', { cls: 'vaultquery-help-code', text: "heading_text" });
  const row37 = tbody6.createEl('tr');
  const td41 = row37.createEl('td');
  td41.createEl('code', { cls: 'vaultquery-help-code', text: "list_items" });
  const td42 = row37.createEl('td');
  td42.createEl('code', { cls: 'vaultquery-help-code', text: "content" });
  const para6 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para6.appendText("For other tables (");
  para6.createEl('code', { cls: 'vaultquery-help-code', text: "links" });
  para6.appendText(", ");
  para6.createEl('code', { cls: 'vaultquery-help-code', text: "tags" });
  para6.appendText(", ");
  para6.createEl('code', { cls: 'vaultquery-help-code', text: "table_cells" });
  para6.appendText("), changes are database-only and will be overwritten on the next reindex. This is useful for:");
  const list2 = container.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item9 = list2.createEl('li', { cls: 'vaultquery-help-list-item' });
  item9.appendText("Computed/derived columns");
  const item10 = list2.createEl('li', { cls: 'vaultquery-help-list-item' });
  item10.appendText("Temporary flags or caches");
  const item11 = list2.createEl('li', { cls: 'vaultquery-help-list-item' });
  item11.appendText("Cross-referencing data");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Auto-set Icon Based on Folder" });
  const para7 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para7.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para7.appendText(" When a note is created in \"Projects/\", set ");
  para7.createEl('code', { cls: 'vaultquery-help-code', text: "icon: 📁" });
  para7.appendText(" in frontmatter.");
  const heading1 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading1.appendText("Using ");
  heading1.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_property" });
  heading1.appendText(" (Modifies File)");
  const codeBlock = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code = codeBlock.createEl('code');
  renderPrismCode(code, "CREATE TRIGGER auto_project_icon\nAFTER INSERT ON notes\nWHEN NEW.path LIKE 'Projects/%'\nBEGIN\n  SELECT vq_set_property(NEW.path, 'icon', '📁');\nEND;", "vaultquery-trigger");
  codeBlock.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para8 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para8.appendText("This adds ");
  para8.createEl('code', { cls: 'vaultquery-help-code', text: "icon: 📁" });
  para8.appendText(" to the file's frontmatter.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Using Direct SQL (Database-Only)" });
  const codeBlock1 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock1.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code1 = codeBlock1.createEl('code');
  renderPrismCode(code1, "CREATE TRIGGER auto_project_icon_db\nAFTER INSERT ON notes\nWHEN NEW.path LIKE 'Projects/%'\nBEGIN\n  INSERT OR REPLACE INTO properties (path, key, value, value_type, array_index)\n  VALUES (NEW.path, 'icon', '📁', 'string', NULL);\nEND;", "vaultquery-trigger");
  codeBlock1.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para9 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para9.appendText("This updates the database but the file will not have the ");
  para9.createEl('code', { cls: 'vaultquery-help-code', text: "icon" });
  para9.appendText(" property. The change persists until the file is reindexed.");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Auto-rename Untitled Notes" });
  const para10 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para10.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para10.appendText(" When a note is created in \"Daily Notes/\" with name \"Untitled\", rename it to today's date.");
  const heading2 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading2.appendText("Using ");
  heading2.createEl('code', { cls: 'vaultquery-help-code', text: "vq_rename_note" });
  heading2.appendText(" (Modifies File)");
  const codeBlock2 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock2.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code2 = codeBlock2.createEl('code');
  renderPrismCode(code2, "CREATE TRIGGER auto_rename_untitled\nAFTER INSERT ON notes\nWHEN NEW.path LIKE 'Daily Notes/%'\n  AND NEW.title = 'Untitled'\nBEGIN\n  SELECT vq_rename_note(\n    NEW.path,\n    strftime('%Y-%m-%d', 'now')\n  );\nEND;", "vaultquery-trigger");
  codeBlock2.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para11 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para11.appendText("This actually renames the file on disk.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Using Direct SQL (Database-Only)" });
  const codeBlock3 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock3.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code3 = codeBlock3.createEl('code');
  renderPrismCode(code3, "CREATE TRIGGER auto_rename_untitled_db\nAFTER INSERT ON notes\nWHEN NEW.path LIKE 'Daily Notes/%'\n  AND NEW.title = 'Untitled'\nBEGIN\n  UPDATE notes\n  SET title = strftime('%Y-%m-%d', 'now')\n  WHERE path = NEW.path;\nEND;", "vaultquery-trigger");
  codeBlock3.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para12 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para12.appendText("This only changes the ");
  para12.createEl('code', { cls: 'vaultquery-help-code', text: "title" });
  para12.appendText(" column in the database. The actual file remains named \"Untitled.md\" and will revert on reindex.");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Replace Date Placeholders" });
  const para13 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para13.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para13.appendText(" When a note contains ");
  para13.createEl('code', { cls: 'vaultquery-help-code', text: "{{today}}" });
  para13.appendText(", replace it with today's date (e.g., \"2026-01-06\").");
  const callout2 = container.createDiv({ cls: 'callout', attr: { 'data-callout': "warning", 'data-callout-metadata': '', 'data-callout-fold': '' } });
  const header2 = callout2.createDiv({ cls: 'callout-title', attr: { dir: 'auto' } });
  const icon2 = header2.createDiv({ cls: 'callout-icon' });
  ctx.setIcon(icon2, "alert-triangle");
  header2.createDiv({ cls: 'callout-title-inner', text: "Avoid Self-Modification" });
  const content2 = callout2.createDiv({ cls: 'callout-content' });
  const para14 = content2.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para14.appendText("If the note containing the trigger also contains the placeholder text (like ");
  para14.createEl('code', { cls: 'vaultquery-help-code', text: "{{today}}" });
  para14.appendText(" in the trigger SQL), the trigger could modify itself. Use ");
  para14.createEl('code', { cls: 'vaultquery-help-code', text: "{this.path}" });
  para14.appendText(" to automatically exclude the trigger's own file.");
  const heading3 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading3.appendText("Using ");
  heading3.createEl('code', { cls: 'vaultquery-help-code', text: "vq_replace_content" });
  heading3.appendText(" (Modifies File)");
  const para15 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para15.appendText("Use AFTER INSERT to run when a new note is created:");
  const codeBlock4 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock4.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code4 = codeBlock4.createEl('code');
  renderPrismCode(code4, "CREATE TRIGGER replace_today_on_create\nAFTER INSERT ON notes\nWHEN NEW.content LIKE '%{{today}}%'\n  AND NEW.path != '{this.path}'\nBEGIN\n  SELECT vq_replace_content(\n    NEW.path,\n    '{{today}}',\n    strftime('%Y-%m-%d', 'now')\n  );\nEND;", "vaultquery-trigger");
  codeBlock4.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para16 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para16.appendText("Or use AFTER UPDATE to run when an existing note is edited:");
  const codeBlock5 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock5.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code5 = codeBlock5.createEl('code');
  renderPrismCode(code5, "CREATE TRIGGER replace_today_on_edit\nAFTER UPDATE ON notes\nWHEN NEW.content LIKE '%{{today}}%'\n  AND NEW.path != '{this.path}'\nBEGIN\n  SELECT vq_replace_content(\n    NEW.path,\n    '{{today}}',\n    strftime('%Y-%m-%d', 'now')\n  );\nEND;", "vaultquery-trigger");
  codeBlock5.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para17 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para17.appendText("Both triggers modify the actual file content. Use AFTER INSERT for new notes only, AFTER UPDATE for edits only, or create both for complete coverage. The ");
  para17.createEl('code', { cls: 'vaultquery-help-code', text: "{this.path}" });
  para17.appendText(" placeholder is automatically replaced with the path of the note containing the trigger.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Using Direct SQL (Auto-Synced)" });
  const para18 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para18.appendText("Use AFTER INSERT for new notes:");
  const codeBlock6 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock6.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code6 = codeBlock6.createEl('code');
  renderPrismCode(code6, "CREATE TRIGGER replace_today_db\nAFTER INSERT ON notes\nWHEN NEW.content LIKE '%{{today}}%'\n  AND NEW.path != '{this.path}'\nBEGIN\n  UPDATE notes\n  SET content = replace(content, '{{today}}', strftime('%Y-%m-%d', 'now'))\n  WHERE path = NEW.path;\nEND;", "vaultquery-trigger");
  codeBlock6.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para19 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para19.appendText("Or AFTER UPDATE for existing notes:");
  const codeBlock7 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock7.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code7 = codeBlock7.createEl('code');
  renderPrismCode(code7, "CREATE TRIGGER replace_today_db_update\nAFTER UPDATE ON notes\nWHEN NEW.content LIKE '%{{today}}%'\n  AND NEW.path != '{this.path}'\nBEGIN\n  UPDATE notes\n  SET content = replace(content, '{{today}}', strftime('%Y-%m-%d', 'now'))\n  WHERE path = NEW.path;\nEND;", "vaultquery-trigger");
  codeBlock7.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const callout3 = container.createDiv({ cls: 'callout', attr: { 'data-callout': "tip", 'data-callout-metadata': '', 'data-callout-fold': '' } });
  const header3 = callout3.createDiv({ cls: 'callout-title', attr: { dir: 'auto' } });
  const icon3 = header3.createDiv({ cls: 'callout-icon' });
  ctx.setIcon(icon3, "flame");
  header3.createDiv({ cls: 'callout-title-inner', text: "Auto-Sync" });
  const content3 = callout3.createDiv({ cls: 'callout-content' });
  const para20 = content3.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para20.appendText("When triggers modify ");
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "notes.content" });
  para20.appendText(", ");
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "properties" });
  para20.appendText(", ");
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "tasks" });
  para20.appendText(", ");
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "headings" });
  para20.appendText(", or ");
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "list_items" });
  para20.appendText(", changes are automatically synced back to files. This means direct SQL modifications are persistent, just like using ");
  para20.createEl('code', { cls: 'vaultquery-help-code', text: "vq_*" });
  para20.appendText(" functions.");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Notify on Task Completion" });
  const para21 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para21.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para21.appendText(" Show a notification when a task is marked as DONE.");
  const heading4 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading4.appendText("Using ");
  heading4.createEl('code', { cls: 'vaultquery-help-code', text: "vq_notify" });
  const codeBlock8 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock8.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code8 = codeBlock8.createEl('code');
  renderPrismCode(code8, "CREATE TRIGGER task_completed_notify\nAFTER UPDATE ON tasks\nWHEN OLD.status != 'DONE' AND NEW.status = 'DONE'\nBEGIN\n  SELECT vq_notify('Task completed: ' || substr(NEW.task_text, 1, 50));\nEND;", "vaultquery-trigger");
  codeBlock8.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const heading5 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading5.appendText("Using ");
  heading5.createEl('code', { cls: 'vaultquery-help-code', text: "vq_log" });
  heading5.appendText(" (Console Only)");
  const codeBlock9 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock9.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code9 = codeBlock9.createEl('code');
  renderPrismCode(code9, "CREATE TRIGGER task_completed_log\nAFTER UPDATE ON tasks\nWHEN OLD.status != 'DONE' AND NEW.status = 'DONE'\nBEGIN\n  SELECT vq_log('Task completed: ' || NEW.task_text);\nEND;", "vaultquery-trigger");
  codeBlock9.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Computed Column - Days Until Due" });
  const para22 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para22.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para22.appendText(" Maintain a computed column tracking days until a task is due.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Using Direct SQL" });
  const codeBlock10 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock10.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code10 = codeBlock10.createEl('code');
  renderPrismCode(code10, "CREATE TRIGGER update_days_until_due\nAFTER INSERT ON tasks\nWHEN NEW.due_date IS NOT NULL\nBEGIN\n  -- This is a database-only computation\n  -- Query this later for sorting/filtering\n  SELECT vq_log(\n    'Task due in ' ||\n    (julianday(NEW.due_date) - julianday('now')) ||\n    ' days: ' || NEW.task_text\n  );\nEND;", "vaultquery-trigger");
  codeBlock10.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const callout4 = container.createDiv({ cls: 'callout', attr: { 'data-callout': "note", 'data-callout-metadata': '', 'data-callout-fold': '' } });
  const header4 = callout4.createDiv({ cls: 'callout-title', attr: { dir: 'auto' } });
  const icon4 = header4.createDiv({ cls: 'callout-icon' });
  ctx.setIcon(icon4, "pencil");
  header4.createDiv({ cls: 'callout-title-inner', text: "Note" });
  const content4 = callout4.createDiv({ cls: 'callout-content' });
  const para23 = content4.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para23.appendText("For truly computed columns, consider using a VIEW instead of a trigger.");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Cross-Reference Tags" });
  const para24 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para24.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para24.appendText(" When a note gets a #project tag, ensure it has a ");
  para24.createEl('code', { cls: 'vaultquery-help-code', text: "status" });
  para24.appendText(" property.");
  const heading6 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading6.appendText("Using ");
  heading6.createEl('code', { cls: 'vaultquery-help-code', text: "vq_set_property" });
  heading6.appendText(" (Modifies File)");
  const codeBlock11 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock11.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code11 = codeBlock11.createEl('code');
  renderPrismCode(code11, "CREATE TRIGGER ensure_project_status\nAFTER INSERT ON tags\nWHEN NEW.tag_name = '#project'\n  AND NOT EXISTS (\n    SELECT 1 FROM properties\n    WHERE path = NEW.path AND key = 'status'\n  )\nBEGIN\n  SELECT vq_set_property(NEW.path, 'status', 'active');\nEND;", "vaultquery-trigger");
  codeBlock11.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Using Direct SQL (Database-Only)" });
  const codeBlock12 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock12.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code12 = codeBlock12.createEl('code');
  renderPrismCode(code12, "CREATE TRIGGER ensure_project_status_db\nAFTER INSERT ON tags\nWHEN NEW.tag_name = '#project'\n  AND NOT EXISTS (\n    SELECT 1 FROM properties\n    WHERE path = NEW.path AND key = 'status'\n  )\nBEGIN\n  INSERT INTO properties (path, key, value, value_type, array_index)\n  VALUES (NEW.path, 'status', 'active', 'string', NULL);\nEND;", "vaultquery-trigger");
  codeBlock12.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Example: Debouncing Triggers" });
  const para25 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para25.createEl('strong', { cls: 'vaultquery-help-bold', text: "Goal:" });
  para25.appendText(" Prevent a trigger from firing repeatedly while the user is still typing.");
  const para26 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para26.appendText("When editing a heading, each keystroke can trigger a reindex. Without debouncing, the trigger might fire dozens of times while typing a single heading. Use ");
  para26.createEl('code', { cls: 'vaultquery-help-code', text: "vq_debounce(key, ms)" });
  para26.appendText(" to only execute the trigger if enough time has passed.");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Global Debounce (Per-Trigger)" });
  const para27 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para27.appendText("Only fire once per 500ms, regardless of which file changed:");
  const codeBlock13 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock13.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code13 = codeBlock13.createEl('code');
  renderPrismCode(code13, "CREATE TRIGGER format_heading_debounced\nAFTER INSERT ON headings\nWHEN vq_debounce('format_heading', 500)\nBEGIN\n  SELECT vq_log('Heading inserted: ' || NEW.heading_text);\nEND;", "vaultquery-trigger");
  codeBlock13.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Per-Path Debounce" });
  const para28 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para28.appendText("Debounce separately for each file (one file typing won't block another):");
  const codeBlock14 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock14.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code14 = codeBlock14.createEl('code');
  renderPrismCode(code14, "CREATE TRIGGER format_heading_per_file\nAFTER INSERT ON headings\nWHEN vq_debounce('format_heading:' || NEW.path, 500)\nBEGIN\n  SELECT vq_log('Heading inserted in ' || NEW.path || ': ' || NEW.heading_text);\nEND;", "vaultquery-trigger");
  codeBlock14.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Combining with Other Conditions" });
  const para29 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para29.appendText("Use ");
  para29.createEl('code', { cls: 'vaultquery-help-code', text: "AND" });
  para29.appendText(" to combine debounce with other conditions:");
  const codeBlock15 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock15.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code15 = codeBlock15.createEl('code');
  renderPrismCode(code15, "CREATE TRIGGER auto_tag_project_debounced\nAFTER INSERT ON headings\nWHEN NEW.heading_text LIKE 'Project:%'\n  AND vq_debounce('auto_tag:' || NEW.path, 1000)\nBEGIN\n  SELECT vq_set_property(NEW.path, 'type', 'project');\nEND;", "vaultquery-trigger");
  codeBlock15.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const callout5 = container.createDiv({ cls: 'callout', attr: { 'data-callout': "tip", 'data-callout-metadata': '', 'data-callout-fold': '' } });
  const header5 = callout5.createDiv({ cls: 'callout-title', attr: { dir: 'auto' } });
  const icon5 = header5.createDiv({ cls: 'callout-icon' });
  ctx.setIcon(icon5, "flame");
  header5.createDiv({ cls: 'callout-title-inner', text: "Choosing Debounce Time" });
  const content5 = callout5.createDiv({ cls: 'callout-content' });
  const list3 = content5.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item12 = list3.createEl('li', { cls: 'vaultquery-help-list-item' });
  item12.createEl('strong', { cls: 'vaultquery-help-bold', text: "300-500ms" });
  item12.appendText(" - Good for most typing scenarios");
  const item13 = list3.createEl('li', { cls: 'vaultquery-help-list-item' });
  item13.createEl('strong', { cls: 'vaultquery-help-bold', text: "1000ms+" });
  item13.appendText(" - For expensive operations that should run rarely");
  const item14 = list3.createEl('li', { cls: 'vaultquery-help-list-item' });
  item14.createEl('strong', { cls: 'vaultquery-help-bold', text: "Per-path debounce" });
  item14.appendText(" - Usually preferred so editing one file doesn't block triggers in another");
  const heading7 = container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2' });
  heading7.appendText("Trailing-Edge Debounce with ");
  heading7.createEl('code', { cls: 'vaultquery-help-code', text: "vq_defer" });
  const para30 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para30.createEl('code', { cls: 'vaultquery-help-code', text: "vq_debounce" });
  para30.appendText(" uses ");
  para30.createEl('strong', { cls: 'vaultquery-help-bold', text: "leading-edge" });
  para30.appendText(" behavior: the trigger fires immediately on the first event, then blocks subsequent events until the cooldown passes.");
  const para31 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para31.createEl('code', { cls: 'vaultquery-help-code', text: "vq_defer" });
  para31.appendText(" uses ");
  para31.createEl('strong', { cls: 'vaultquery-help-bold', text: "trailing-edge" });
  para31.appendText(" behavior: the trigger waits until activity stops for the specified duration, then executes once with the final state. This is useful for waiting until typing stops before taking action.");
  const codeBlock16 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock16.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code16 = codeBlock16.createEl('code');
  renderPrismCode(code16, "CREATE TRIGGER format_heading_after_idle\nAFTER INSERT ON headings\nBEGIN\n  -- Defer all actions until 3 seconds of idle time\n  SELECT vq_defer('heading:' || NEW.path, 3000);\n\n  -- These actions will only execute after 3 seconds with no new headings\n  SELECT vq_log('Heading finalized: ' || NEW.heading_text);\n  SELECT vq_set_property(NEW.path, 'last_heading', NEW.heading_text);\nEND;", "vaultquery-trigger");
  codeBlock16.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  const para32 = container.createEl('p', { cls: 'vaultquery-help-paragraph' });
  para32.createEl('strong', { cls: 'vaultquery-help-bold', text: "Key differences:" });
  const list4 = container.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item15 = list4.createEl('li', { cls: 'vaultquery-help-list-item' });
  item15.createEl('code', { cls: 'vaultquery-help-code', text: "vq_debounce" });
  item15.appendText(" → Executes immediately, blocks repeats (use in WHEN clause)");
  const item16 = list4.createEl('li', { cls: 'vaultquery-help-list-item' });
  item16.createEl('code', { cls: 'vaultquery-help-code', text: "vq_defer" });
  item16.appendText(" → Waits for idle, then executes (use in BEGIN block)");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Trigger Syntax Reference" });
  const codeBlock17 = container.createEl('pre', { cls: 'HyperMD-codeblock HyperMD-codeblock-bg display-only' });
  codeBlock17.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "```vaultquery-trigger\n" });
  const code17 = codeBlock17.createEl('code');
  renderPrismCode(code17, "CREATE TRIGGER trigger_name\n{BEFORE | AFTER} {INSERT | UPDATE | DELETE} ON table_name\n[WHEN condition]\nBEGIN\n  -- SQL statements and/or vq_* function calls\nEND;", "vaultquery-trigger");
  codeBlock17.createSpan({ cls: 'vaultquery-help-fence cm-formatting', text: "\n```" });
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Supported Tables" });
  const list5 = container.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item17 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item17.createEl('code', { cls: 'vaultquery-help-code', text: "notes" });
  item17.appendText(" - Note metadata (path, title, content, created, modified, size)");
  const item18 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item18.createEl('code', { cls: 'vaultquery-help-code', text: "properties" });
  item18.appendText(" - Frontmatter properties");
  const item19 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item19.createEl('code', { cls: 'vaultquery-help-code', text: "tasks" });
  item19.appendText(" - Tasks with status, dates, etc.");
  const item20 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item20.createEl('code', { cls: 'vaultquery-help-code', text: "headings" });
  item20.appendText(" - Heading text and levels");
  const item21 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item21.createEl('code', { cls: 'vaultquery-help-code', text: "links" });
  item21.appendText(" - Internal and external links");
  const item22 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item22.createEl('code', { cls: 'vaultquery-help-code', text: "tags" });
  item22.appendText(" - Inline tags");
  const item23 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item23.createEl('code', { cls: 'vaultquery-help-code', text: "list_items" });
  item23.appendText(" - List items");
  const item24 = list5.createEl('li', { cls: 'vaultquery-help-list-item' });
  item24.createEl('code', { cls: 'vaultquery-help-code', text: "table_cells" });
  item24.appendText(" - Markdown table data");
  container.createEl('h2', { cls: 'vaultquery-help-heading vaultquery-help-h2', text: "Special References" });
  const list6 = container.createEl('ul', { cls: 'vaultquery-help-list vaultquery-help-unordered-list' });
  const item25 = list6.createEl('li', { cls: 'vaultquery-help-list-item' });
  item25.createEl('code', { cls: 'vaultquery-help-code', text: "NEW.column" });
  item25.appendText(" - New value (INSERT/UPDATE)");
  const item26 = list6.createEl('li', { cls: 'vaultquery-help-list-item' });
  item26.createEl('code', { cls: 'vaultquery-help-code', text: "OLD.column" });
  item26.appendText(" - Previous value (UPDATE/DELETE)");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Tips & Best Practices" });
  const list7 = container.createEl('ol', { cls: 'vaultquery-help-list vaultquery-help-ordered-list' });
  const item27 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item27.createEl('strong', { cls: 'vaultquery-help-bold', text: "Use `{this.path}` to prevent self-modification" });
  item27.appendText(" - Triggers containing placeholder text can modify themselves");
  const item28 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item28.createEl('strong', { cls: 'vaultquery-help-bold', text: "Test with `vq_log` first" });
  item28.appendText(" - Verify trigger conditions before adding actions");
  const item29 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item29.createEl('strong', { cls: 'vaultquery-help-bold', text: "Be specific with conditions" });
  item29.appendText(" - Use precise WHEN clauses to avoid unintended matches");
  const item30 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item30.createEl('strong', { cls: 'vaultquery-help-bold', text: "Check for existing values" });
  item30.appendText(" - Avoid setting properties that already have the desired value");
  const item31 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item31.createEl('strong', { cls: 'vaultquery-help-bold', text: "One action per trigger" });
  item31.appendText(" - Keep triggers focused; create multiple triggers for complex logic");
  const item32 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item32.createEl('strong', { cls: 'vaultquery-help-bold', text: "Direct SQL on `notes`, `properties`, `tasks`, `headings`, `list_items` auto-syncs" });
  item32.appendText(" - Changes are written back to files");
  const item33 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item33.createEl('strong', { cls: 'vaultquery-help-bold', text: "Use both INSERT and UPDATE triggers for full coverage" });
  item33.appendText(" - AFTER INSERT fires for new notes, AFTER UPDATE fires for edits");
  const item34 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item34.createEl('strong', { cls: 'vaultquery-help-bold', text: "Trigger names are global" });
  item34.appendText(" - If two notes define a trigger with the same name, the last one loaded wins. Use unique, descriptive names like ");
  item34.createEl('code', { cls: 'vaultquery-help-code', text: "auto_project_icon_on_create" });
  const item35 = list7.createEl('li', { cls: 'vaultquery-help-list-item' });
  item35.createEl('strong', { cls: 'vaultquery-help-bold', text: "Debounce typing-sensitive triggers" });
  item35.appendText(" - Use ");
  item35.createEl('code', { cls: 'vaultquery-help-code', text: "vq_debounce('key', 500)" });
  item35.appendText(" in WHEN clause to prevent triggers from firing on every keystroke");
  container.createEl('h1', { cls: 'vaultquery-help-heading vaultquery-help-h1', text: "Registered Triggers" });
  try { ctx.renderDynamic(container, "triggers"); } catch (e) { console.error('Failed to render dynamic content:', "triggers", e); }
}

type PrismTokenStream = string | PrismToken | PrismTokenStream[];

interface PrismToken {
  type: string;
  content: PrismTokenStream;
  alias?: string | string[];
}

interface PrismApi {
  languages: Record<string, unknown>;
  tokenize: (code: string, grammar: unknown) => PrismTokenStream[];
}

declare const Prism: PrismApi | undefined;

function renderPrismCode(container: HTMLElement, source: string, language: string): void {
  const prism = typeof Prism !== 'undefined' ? Prism : undefined;
  const grammar = prism?.languages[language];

  if (!prism || !grammar) {
    container.appendText(source);
    return;
  }

  appendPrismTokens(container, prism.tokenize(source, grammar));
}

function appendPrismTokens(container: HTMLElement, tokenStream: PrismTokenStream): void {
  if (typeof tokenStream === 'string') {
    container.appendText(tokenStream);
    return;
  }

  if (Array.isArray(tokenStream)) {
    for (const token of tokenStream) {
      appendPrismTokens(container, token);
    }
    return;
  }

  const aliases = Array.isArray(tokenStream.alias)
    ? tokenStream.alias
    : tokenStream.alias ? [tokenStream.alias] : [];
  const span = container.createSpan({ cls: ['token', tokenStream.type, ...aliases].join(' ') });
  appendPrismTokens(span, tokenStream.content);
}